# Server Configuration
PORT=3001

# Stellar / Soroban Configuration
STELLAR_NETWORK=TESTNET
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015
FEE_PAYER_SECRET=S... (Stellar Secret Key)
SOROBAN_CONTRACT_IDS=  # Comma-separated contract IDs for event filtering (optional)

# Event Bridge
EVENT_BRIDGE_POLL_MS=5000
EVENT_BRIDGE_ERROR_BACKOFF_MS=10000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/zaps?schema=public

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Worker / BullMQ
WORKER_CONCURRENCY=5
JOB_MAX_RETRIES=3
JOB_BACKOFF_DELAY_MS=1000

# Security
JWT_SECRET=super-secret-key
